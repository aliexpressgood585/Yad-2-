/**
 * הפלטה כערכים מוחלטים.
 *
 * בדרך כלל צבע מגיע מטוקן CSS (`hsl(var(--primary))`), וזה הכלל. הקובץ
 * הזה קיים בשביל ארבעה הקשרים שלא מריצים CSS ולכן לא יכולים לקרוא טוקן:
 *
 *   next/og      — Satori מרנדר ל-PNG ולא מכיר var()
 *   manifest.ts  — מניפסט PWA הוא JSON
 *   email.ts     — לקוחות דוא"ל לא תומכים במשתני CSS
 *   MapLibre     — מאפייני paint מקבלים מחרוזת צבע, לא CSS
 *
 * הערכים חייבים להישאר תואמים ל-globals.css. שינוי כאן בלי שינוי שם
 * (או להפך) הוא באג — ראה DESIGN.md.
 */

/** חוגה בהירה. זו הערכה שדוא"ל ומניפסט מקבלים: שניהם בהירים כברירת מחדל. */
export const PALETTE = {
  ink: "#14171A",
  bone: "#F7F5F0",
  /** משטח מוגבה על החוגה — הכרטיס, וגוף הודעת הדוא"ל */
  surface: "#FFFFFF",
  /**
   * צבע הפעולה בחוגה בהירה — ענבר מעומק לברונזה.
   * הענבר הזוהר נופל מתחת ל-AA על רקע עצם, ולכן הוא שמור לסקאלה.
   */
  amber: "#005F3C",
  verdict: "#0B4A8F",
  hair: "#DDD8CE",
  alarm: "#C2410C",
  /** טקסט משני — --muted-foreground בבהיר */
  muted: "#585C60",
  /*
   * קבועי הסקאלה — זהים ל-`--scale-plate` / `--scale-hair` / `--needle`
   * ב-globals.css ול-`MARK_COLORS` בסימן. הם אינם תלויים בחוגה: לוחית
   * המכשיר נראית אותו דבר בבהיר ובכהה, וזה מה שהופך אותה לאובייקט אחד
   * ולא לרכיב שמשנה זהות לפי הרקע.
   */
  plate: "#1C1F23",
  scaleHair: "#474D54",
  needle: "#2FB87A",
} as const;

/** חוגה כהה — הערכה הקנונית של הכיוון. */
export const PALETTE_DARK = {
  ink: "#E6E2D6",
  bone: "#16181B",
  surface: "#1E2126",
  amber: "#FFB000",
  verdict: "#58C6D8",
  hair: "#32363C",
  alarm: "#E8734A",
  muted: "#989DA4",
} as const;

/**
 * קבועי הסקאלה. אינם תלויים בערכת הצבעים ואינם צבעי ממשק —
 * לוחית החוגה כהה תמיד, וזה מה שמאפשר למחוג לשמור על אותה עוצמה
 * בבהיר ובכהה. ראה את ההערה ב-`:root` ב-globals.css.
 */
export const SCALE = {
  plate: "#16181B",
  hair: "#3A3F46",
  needle: "#FFB000",
} as const;
