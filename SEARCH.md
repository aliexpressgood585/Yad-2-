# חיפוש בעברית

מנוע החיפוש הוא הליבה התחרותית של המוצר. המסמך הזה מתעד מה נבנה, למה,
ואיפה הגבולות.

הרצת הבדיקות: `npm run check:search`

---

## למה חיפוש בעברית שונה

באנגלית מספיק לחתוך רווחים ולהוריד רישיות. בעברית יש ארבע בעיות שכל
אחת מהן לבדה מספיקה כדי להחזיר אפס תוצאות לשאילתה תקינה:

| בעיה | דוגמה | מה קורה בלי טיפול |
| --- | --- | --- |
| אותיות סופיות | ירושלים / ירושלימ | שתי מילים שונות לאינדקס |
| גרשיים | ת״א · ת"א · ת'א · תא | ארבע צורות, אף אחת לא מוצאת את השאר |
| כתיב מלא/חסר | מזגן / מזגון | לא נפגשות |
| מספרים מילוליים | 3 חדרים / שלושה חדרים | לא נפגשות |

**מלכודת שתפסה אותנו בפועל:** `\b` של JavaScript מוגדר מול `\w`, שאינו
כולל אותיות עבריות. `/\bמיליון\b/` **לעולם לא מתאים**. כל הביטויים
הרגולריים ב-`parse-query.ts` משתמשים בגבולות מפורשים.

---

## 1. נרמול — `src/lib/search/normalize.ts`

`normalize()` היא הפונקציה היחידה שנוגעת בטקסט לפני אינדוקס. אם צד
אחד עובר נרמול שונה מהשני, החיפוש נשבר בשקט.

סדר הפעולות משמעותי:

1. NFKD + הסרת ניקוד וטעמים
2. מחיקת גרשיים — `ת״א` → `תא`, ולא `ת א`
3. **מקף:** נמחק רק בין אותיות בודדות (`י-ם` → `ים`), אחרת הופך לרווח
   (`תל אביב-יפו` → `תל אביב יפו`). כלל אחד לשני הכיוונים היה שובר את
   אחד מהם.
4. אותיות סופיות → רגילות
5. כל מה שאינו אות/ספרה → רווח. נקודה נשמרת רק בין ספרות (`1.5 מיליון`)

`tokenize()` מוסיף מעל זה החלפות ברמת מילה שלמה: מספרים מילוליים
וכתיב. הן חיות שם ולא ב-`normalize` כי הן פועלות על מילה, לא על תו.

**רשימת חסימה למספרים מילוליים:** `באר שבע` אינו `באר 7`.

---

## 2. מילון — `src/lib/search/synonyms.ts`

**398 מונחים** בקבוצות שקילות דו-כיווניות. כל מונח בקבוצה מרחיב לכל
השאר, כך שאין צורך בשתי מפות.

מכסה: יצרני רכב בשתי השפות · קיצורי ערים (`ת״א`, `פ״ת`, `ר״ג`, `י-ם`) ·
מונחי נדל"ן (`ממ״ד`, `יח״ד`, `דירת גג`) · מוצרי יד שנייה
(`פריג'ידר`↔`מקרר`, `פלאפון`↔`סלולרי`) · שגיאות שהשתרשו (`פולסווגן`).

חיפוש באנגלית מוצא מודעה בעברית ולהפך — `toyota` → `טויוטה`,
`bugaboo` → `בוגבו`.

---

## 3. הבנת שאילתה — `src/lib/search/parse-query.ts`

```
"דירת 3 חד' בת״א עד 6000"
  → rooms: 3 · priceMax: 6,000 · city: תל אביב-יפו · text: "דירה"
```

**מבוסס חוקים ולא מודל.** החיפוש בנתיב החם של כל בקשה; קריאה למודל שם
מוסיפה מאות מילישניות ונקודת כשל חיצונית. חוקים גם ניתנים להסבר
למשתמש — וזה תנאי לכך שיוכל להסיר חילוץ שגוי.

חוקים: חדרים · מ״ר · יד · `עד X` / `מעל X` עם מכפילים (`2 מיליון`) ·
שנה או טווח שנים · עיר (כולל תחיליות `ב`/`ל`/`מ` ושמות מהמילון) · יצרן.

**סדר הכללים משמעותי:** טווח שנים חייב לרוץ לפני `עד X`, אחרת
`"רכב 2015 עד 2018"` נבלע כמחיר מקסימלי.

כל חילוץ מייצר צ'יפ עם `source` — המילים המקוריות שנצרכו — כך
ש-`removeChip()` מחזירה אותן לחיפוש החופשי במקום לאבד אותן.

---

## מצב מול האפיון

| סעיף | מצב |
| --- | --- |
| 1 נרמול | ✅ |
| 2 מילון (400+) | ✅ 398 מונחים |
| 3 חילוץ פילטרים | ✅ הליבה. `category` ו-`neighborhood` לא מחולצים |
| 9 בדיקות | ✅ 43 עוברות |
| 4 סבילות לשגיאות כתיב | ⚠️ `pg_trgm` קיים מהשלב הקודם, בלי סף 0.35 ובלי "חיפשת אולי" |
| 5 `search_vector` + trigger | ❌ עדיין `to_tsvector` בזמן שאילתה |
| 6 השלמה אוטומטית | ❌ |
| 7 הרפיה הדרגתית | ❌ |
| 8 `SearchLog` | ❌ |
| 10 מבחן עומס 100K | ❌ |

**ה-parser עדיין לא מחובר לנתיב החיפוש** — הוא נבנה ונבדק כיחידה
עצמאית. החיבור הוא השלב הבא: `browse-view` צריך להזין את
`ParsedQuery` לתוך `SearchQuery`, והצ'יפים צריכים להיכנס ל-UI לצד
`ActiveFilterChips` הקיים.
