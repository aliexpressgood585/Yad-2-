# חסמים ומגבלות ידועות

לא נותרו חסמים פתוחים — כל ה-Milestones הושלמו, הבנייה עוברת נקייה,
ובדיקת הקבלה מקצה לקצה (`npm run e2e`) עוברת במלואה.

הרשימה כאן מתעדת מגבלות מודעות והחלטות שנדחו למכוון, כדי שיהיה ברור
מה מומש ומה לא.

---

## מגבלות מודעות

### אחסון תמונות בפריסה serverless

ברירת המחדל (`UPLOAD_PROVIDER=local`) שומרת קבצים ב-`public/uploads`.
מערכת הקבצים של Vercel אינה קבועה, ולכן בפרודקשן חובה להגדיר
`UPLOAD_PROVIDER=cloudinary` עם המפתחות. הקוד תומך בשניהם, וההחלפה היא
משתנה סביבה בלבד.

### Rate limiting בזיכרון

ללא `UPSTASH_REDIS_REST_URL`, המגביל פועל בזיכרון של כל מופע בנפרד.
מתאים לפיתוח ולשרת יחיד; בפריסה מרובת מופעים יש להגדיר Upstash כדי
שהמגבלה תהיה אמיתית.

### סליקה

חבילות הקידום נרשמות ומפעילות את הקידום בפועל, אך אין אינטגרציית תשלום.
הזרימה בנויה כך שהוספת ספק סליקה היא הוספת שלב אחד לפני
`POST /api/boosts` — בלי לשנות את המודל או את ה-UI.

### כתובות פנימיות בפידים של סוחרים

כתובת פיד היא קלט של משתמש שהשרת שלנו פונה אליו. הקוד מגביל פרוטוקול
(http/https בלבד), גודל (10MB) וזמן (20 שניות), אבל **אינו חוסם
כתובות רשת פנימיות**. חסימה כזו שייכת לשכבת הרשת של סביבת ההרצה:
בדיקת DNS בקוד אינה עמידה בפני DNS rebinding, והיא הייתה נותנת ביטחון
שאינו קיים. בפריסה שבה זה חשוב יש להגדיר egress policy.

### תדירות מסירת ההתראות

Vercel בתוכנית החינם מרשה משימת cron יומית אחת (`DECISIONS.md` §15),
ולכן התקציר המרוכז נשלח פעם ביום. הודעת צ׳אט אינה מושפעת — היא נשלחת
מיד דרך `deliverNow` — והתראה שנדחתה בשעות שקט מגיעה גם כשהמשתמש נכנס
ללוח, כי `GET /api/notifications` מרוקן את התור שלו. מתזמן תכוף יותר
ישפר את זמן המסירה של תקצירים בלבד.

### חגי ישראל בשעות השקט

שעות השקט מכסות לילה ושבת. חגים אינם מכוסים — הם דורשים לוח שנה עברי
מלא, וזה פרויקט בפני עצמו. מתועד ב-`GROWTH.md` סעיף B.

### צ'אט בפולינג

הודעות נמשכות כל 8 שניות (ראה `DECISIONS.md` §7). מספיק למו״מ על מודעה,
אבל אינו זמן אמת מלא. מעבר ל-WebSocket דורש שירות חיצוני בפריסה serverless.

### חיפוש בעברית

אין ל-Postgres מילון stemming לעברית, ולכן נטיות מכוסות חלקית בלבד
(התאמת תחילית + trigram + מילים נרדפות). "דירה" לא תמצא "דירת" דרך FTS,
אלא רק דרך דמיון trigram. שיפור אמיתי ידרוש מילון מורפולוגי עברי.

### מפה עם קורא מסך

תצוגת המפה מוגבלת מטבעה בעבודה עם טכנולוגיית סיוע. לכל מודעה במפה יש
ייצוג טקסטואלי מלא ברשימות ובדף המודעה, וכל פעולה אפשרית גם בלעדיה.
מתועד בהצהרת הנגישות.

---

## מה נדחה במכוון

| נושא | למה |
| --- | --- |
| שירות חיפוש חיצוני | Postgres מספיק בסדר הגודל הזה; ראה `DECISIONS.md` §1 |
| חסימה אוטומטית של מודעות חשודות | יוריסטיקות מייצרות false positives; סימון + מודרציה אנושית |
| אימות מסמכי עסק | מחייב תהליך תפעולי ידני שאינו חלק מהמערכת |
| תרגום לשפות נוספות | המוצר מוגדר כעברית-RTL בלבד |

---

## איך מאמתים שהכול עובד

```bash
npm run build          # אפס שגיאות TypeScript ואפס אזהרות ESLint
npm run typecheck
npm start &            # שרת פרודקשן
npm run e2e            # 27 בדיקות קבלה מקצה לקצה
```

בדיקת הקבלה מכסה: הרשמה → אימות OTP → פרסום מודעה עם שדות דינמיים
ותמונה → מציאתה בחיפוש חופשי ובפילטרים → שמירה במועדפים → פנייה למוכר
ותגובה → חשיפת טלפון → דיווח → טיפול בפאנל הניהול → ובקרת הרשאות
(אנונימי, משתמש רגיל, ובעלות על מודעה).
