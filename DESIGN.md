# לוח — מערכת העיצוב

מסמך זה הוא המקור לכללי העיצוב. כל טוקן כאן מוגדר ב-`src/app/globals.css`
או ב-`tailwind.config.ts`, וכל חריגה ממנו בקוד היא באג ולא בחירה.

---

## הבעיה שהעיצוב פותר

לוחות מודעות נראים רע מסיבה מבנית, לא מסיבת טעם. הם מציגים כמות עצומה
של תוכן שהועלה בידי משתמשים — תמונות באיכות, ביחס ובבהירות אקראיים —
ומנסים לפצות על חוסר האחידות בעוד צבע, עוד מסגרת, עוד תג ועוד באנר.
כל תוספת כזו מוסיפה רעש, והרעש מצטבר.

הכיוון ההפוך הוא הפתרון: **מסגרת ויזואלית קפדנית שגורמת גם לתמונה בינונית
להיראות מכובדת.** ככל שהמסגרת אחידה יותר, כך התוכן שבתוכה נסבל יותר.

מכאן נגזרים כל הכללים הבאים. הם מגבילים בכוונה.

---

## מקור ההשראה

**שילוט הדרכים הישראלי והבאוהאוס של תל אביב.**

שני דברים שכל ישראלי מזהה בלי לחשוב, ושאף לוח מודעות לא ניצל. שילוט
הדרכים נותן את הפלטה — ירוק בין-עירוני, כחול עירוני, לבן — והבאוהאוס
נותן את ההיגיון: גריד קפדני, ללא קישוט, פונקציה גלויה.

---

## פלטה

הצבעים מוגדרים כ-HSL ב-`globals.css` כדי ש-Tailwind יוכל לגזור שקיפויות
(`bg-primary/10`).

| טוקן | בהיר | תפקיד |
| --- | --- | --- |
| `--ink` | `#14171A` | טקסט ראשי. כמעט שחור, בנטייה כחלחלה. |
| `--paper` | `#F7F5F0` | רקע. לבן אבן ירושלמית, לא לבן טהור. |
| `--sign` | `#005F3C` | ירוק שילוט בין-עירוני. **צבע הפעולה היחיד.** |
| `--route` | `#0B4A8F` | כחול שילוט עירוני. קישורים ומידע. |
| `--stone` | `#DDD8CE` | גבולות, מפרידים, שלד טעינה. |
| `--signal` | `#C2410C` | אזהרות וירידות מחיר. **שום דבר אחר.** |

### חוק ברזל: צבע פעולה אחד

אם במסך אחד יש שני כפתורים צבעוניים — אחד מהם טועה. פעולה משנית היא
כפתור רפאים או קישור, לא צבע שני. `--route` הוא צבע מידע ואין לו כפתור
מלא בשום מסך.

### מצב לילה

מצב לילה הוא **פלטה שנייה שנבנתה מחדש, לא היפוך.** היפוך היה מכהה את
ירוק השילוט ומאבד ניגודיות; במקום זה `--sign` מתבהר ל-`#2FA875` כדי
לשמור על יחס AA מול הרקע הכהה. `--paper` הופך ל-`#16181A`.

---

## טיפוגרפיה

שלושה גופנים, שלושה תפקידים. הטעינה ב-`layout.tsx`.

| תפקיד | גופן | משקלים | שימוש |
| --- | --- | --- | --- |
| Display | Frank Ruhl Libre | 500 / 700 | כותרות מסך, מחיר ראשי בדף מודעה, מספרים בסטטיסטיקות |
| Body | Assistant | 400 / 600 | כל טקסט הממשק |
| Data | Rubik + `tabular-nums` | 400 / 500 / 700 | מחיר, ק"מ, מ"ר, שנה, טלפון |

**Frank Ruhl Libre בשימוש מצומצם בלבד.** הוא סריף עברי אמיתי, וזה מה
שנותן לדף אופי — אבל סריף על טקסט ממשק צפוף פוגע בקריאוּת.

**Data עם `tabular-nums`** — ספרות ברוחב קבוע, כדי שעמודת מחירים בגריד
תתיישר לאורך השורות במקום לרקוד.

### סקאלה סגורה

```
12 / 14 / 16 / 20 / 26 / 34 / 46
```

אין גדלים אחרים. `text-[15px]` בקוד הוא באג. הסקאלה מוגדרת ב-`fontSize`
של `tailwind.config.ts` ומחליפה את ברירת המחדל של Tailwind, כך שגודל
מחוץ לסקאלה פשוט לא קיים כמחלקה.

הבחנה בין שורות שאינה בגודל נעשית **בצבע**: שורת המפרט בכרטיס ושורת
המיקום שתחתיה שתיהן 12px, ונבדלות ב-`--ink/60` מול `--ink/45`. זה שומר
על הסקאלה ועדיין יוצר היררכיה.

### מספרים בתוך טקסט עברי

כל מספר עטוף ב-`.num`, שמחיל `direction: ltr` + `unicode-bidi: isolate`.
בלעדיהם `₪1,234` מוצג הפוך בתוך משפט עברי.

---

## פורמט מספרים

הכלל חי בקוד ב-`src/lib/format.ts`, שהוא **המקור היחיד** לפורמט מספרים.
שום קומפוננטה לא קוראת ל-`toLocaleString` בעצמה.

מספרים נחלקים לשתי משפחות שנראות זהות בקוד ומתנהגות הפוך על המסך:

| משפחה | דוגמה | מפריד אלפים |
| --- | --- | --- |
| **מדידה** — כמה | `84,000 ק"מ`, `₪27,000` | כן |
| **מזהה** — איזה | `2016`, `קומה 3`, `יד 2` | **לא** |

הבאג שהוליד את הכלל: `BYD סיל 2,016`.

`npm run check:format` מקבע את הכללים.

---

## גריד וצפיפות

- **בסיס 4px** לכל מרווח.
- **רדיוס:** 10px בכרטיסים, 8px בכפתורים, 999px **בצ'יפים בלבד**.

### צל

**צל אחד במערכת:** `0 1px 2px hsl(--ink / .06)`, ו**רק ב-hover**.
במנוחה משטח מופרד בגבול `--stone` ברוחב 1px. ערבוב של כמה עומקי צל הוא
בדיוק מה שגורם ללוח להיראות רועש.

**החריג היחיד** הוא `shadow-overlay`, לשכבות פורטל בלבד — תפריט, דיאלוג,
טולטיפ. הן מרחפות מעל התוכן בלי גבול שמפריד אותן ממנו, וב-`0 1px 2px`
הן נראות כאילו נפלו לתוך הדף. אסור להשתמש בו בכרטיס או במשטח בזרימה.

### שני מצבי צפיפות

המשתמש בוחר, והבחירה נשמרת ב-`localStorage` (`src/stores/density.ts`).

| מצב | תיאור |
| --- | --- |
| `grid` | תמונה גדולה, שתי עמודות בנייד. עיון. |
| `list` | תמונה קטנה מימין, שורה אחת. סריקה מהירה פי 3. |

מצב `list` אינו נוחות — הוא מה שסוחרי רכב ומחפשי דירות עושים בפועל.

---

## נרמול תמונות

כל תמונה עוברת דרך `ListingImage`, בלי יוצא מן הכלל. זה הכלל בעל
ההשפעה הגדולה ביותר על איך הלוח נראה.

1. **יחס 4:3 קבוע** עם `object-fit: cover`. אף פעם לא letterbox, אף פעם
   לא מלבן ריק. גובה קבוע הוא גם מה שמחזיק CLS על 0.
2. **`blurhash` כ-placeholder** — הטעינה מתחילה מהצבע האמיתי של התמונה
   ולא ממלבן אפור.
3. **שכבת scrim** (`.img-scrim`) מתחת לתגיות שיושבות על התמונה. אי אפשר
   לדעת אם פינת התמונה בהירה או כהה, וה-scrim מבטיח קריאוּת בלי לצבוע
   את כל התמונה.
4. **תמונה חסרה** → קווי המתאר של הקטגוריה ב-`--stone`. לא אייקון
   "תמונה שבורה".

---

## כרטיס המודעה

```
[תמונה 4:3 · תג "מקודם" בפינה · לב במיקום קבוע]
₪27,000                        Rubik tabular, 20px, --ink — הכי בולט
[מד המחיר]                     פס 2px, מוצג רק כשיש בסיס להשוואה
BYD סיל 2016                   Assistant 600, 16px, שורה אחת + ellipsis
2016 · יד שנייה · 84,000 ק"מ    3 שדות בלבד, 12px, --ink/60
רמת השרון · לפני יומיים         12px, --ink/45
```

### מה הוסר מהכרטיס במכוון

**דירוג המוכר, ספירת צפיות, מספר התמונות.** שלושתם רעש ברשימה: הם לא
עוזרים לבחור בין מודעות, והם מה שהופך לוח לצפוף. מקומם בדף המודעה,
אחרי שהמשתמש כבר נכנס.

### כלל גלובלי: אין שדה בלי תווית

ערך מספרי עירום אסור להופיע בכרטיס. `₪120,000` לבד הוא מספר; `מחזור
חודשי ₪120,000` הוא מידע. הכלל נאכף בקומפוננטת הכרטיס דרך
`formatAttributeEntry`, שמסמן אילו ערכים מסבירים את עצמם (`יד שנייה`,
`84,000 ק"מ`) ואילו חייבים תווית.

### תג "מקודם"

קטן, בצבע טקסט משני, על רקע כרטיס חצי-שקוף. לא צהוב ולא בולט.
**מודעה מקודמת מנצחת במיקום ברשימה, לא בצעקנות.**

---

## מד המחיר — אלמנט החתימה

הקוד: `src/lib/price-meter.ts`, `src/components/listing/price-meter.tsx`.

בלוח מודעות המחיר לבדו חסר משמעות. `₪27,000` הוא זול או יקר רק ביחס
לרכבים דומים — וזה בדיוק המידע שקונה מחפש ואין לו בשום לוח בישראל.

פס דק ברוחב מלא מתחת למחיר, עם סמן במיקום האחוזון, ומתחתיו
`12% מתחת לחציון` ב-`--sign` או `9% מעל החציון` ב-`--ink/60`.

**כללים שאין לעקוף:**

- פחות מ-8 מודעות להשוואה → הפס **לא מוצג כלל**. עדיף בלי נתון מאשר
  נתון שמומצא משלוש מודעות.
- סטייה של פחות מ-3% מהחציון היא רעש סטטיסטי ולא "מחיר טוב" — גם אז
  לא מוצג כלום.
- החישוב ב-SQL בשאילתה אחת לכל עמוד, לא אחת לכרטיס.

"דומה" = אותה תת-קטגוריה, ובקטגוריות שיש בהן ערך השוואה גם טווח קרוב
שלו: ±3 שנות ייצור ברכב, ±חדר בנדל"ן. הערך מנורמל לעמודה
`Listing.comparableBand` עם אינדקס ייעודי, כדי שההשוואה לא תדרוש
`join` אל טבלת השדות הדינמיים בכל שאילתה.

---

## תנועה

**תקציב הדוק:** מקסימום 180ms, easing `cubic-bezier(.2,0,0,1)`.
שניהם ברירת המחדל ב-`tailwind.config.ts`, כך שקשה לחרוג מהם בטעות.

תנועה מותרת בארבעה מקומות בלבד:

1. פתיחת סדין הפילטרים
2. hover על כרטיס (הצל בלבד)
3. lightbox של הגלריה
4. מעבר skeleton→תוכן (fade 120ms)

**אפס** scroll-reveal. **אפס** parallax. **אפס** טקסט אות-אות.
זה לוח מודעות; אנשים באים לעבוד.

`prefers-reduced-motion` מבטל הכול חוץ מ-opacity.

---

## כתיבה בממשק

- **כפתור אומר מה יקרה:** `פרסם מודעה`, לא `המשך`. הטוסט אחריו:
  `המודעה פורסמה`.
- **שגיאה מסבירה מה לעשות:** `המחיר גבוה מהמותר בקטגוריה. הזן עד
  ₪5,000,000.` לא `שגיאה בשדה`.
- **מצב ריק אומר מה להסיר:** `אין תוצאות. נסה בלי "מעלית" — 31 מודעות
  מתאימות.` עם כפתור שמסיר את הפילטר.
- אפס סימני קריאה. אפס אמוג'ים בממשק. אפס "אופס!".

---

## צבעים מחוץ ל-CSS

ארבעה הקשרים לא מריצים CSS ולכן לא יכולים לקרוא טוקן: `next/og` (Satori),
מניפסט ה-PWA, תבניות הדוא"ל, ומאפייני `paint` של MapLibre.

כולם קוראים מ-`src/lib/palette.ts`. **הערכים שם חייבים להישאר תואמים
ל-`globals.css`** — שינוי בצד אחד בלי הצד השני הוא באג.

---

## RTL

- מאפיינים לוגיים בלבד: `ms-`/`me-`/`start`/`end`. לא `ml-`/`mr-`.
- חצי ניווט הפוכים (חץ "הבא" מצביע שמאלה).
- `<Slider dir="rtl">` — Radix לא יורש כיוון מהמסמך.
- גרפי Recharts עטופים ב-`dir="ltr"` — הספרייה מניחה צירים משמאל לימין.
- מספרים ב-`.num`, ראה למעלה.
