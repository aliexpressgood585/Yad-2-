# שנתות — מערכת העיצוב

מסמך זה הוא המקור לכללי העיצוב. כל טוקן כאן מוגדר ב-`src/app/globals.css`
או ב-`tailwind.config.ts`, וכל חריגה ממנו בקוד היא באג ולא בחירה.

---

## הבעיה שהעיצוב פותר

לוחות מודעות נראים רע מסיבה מבנית, לא מסיבת טעם. הם מציגים כמות עצומה
של תוכן שהועלה בידי משתמשים — תמונות באיכות, ביחס ובבהירות אקראיים —
ומנסים לפצות על חוסר האחידות בעוד צבע, עוד מסגרת, עוד תג ועוד באנר.
כל תוספת כזו מוסיפה רעש, והרעש מצטבר.

הכיוון ההפוך הוא הפתרון: **מסגרת ויזואלית קפדנית שגורמת גם לתמונה בינונית
להיראות מכובדת.** ככל שהמסגרת אחידה יותר, כך התוכן שבתוכה נסבל יותר.

מכאן נגזרים כל הכללים הבאים. הם מגבילים בכוונה.

---

## מקור ההשראה

**מכשיר מדידה.**

מי שמחפש רכב או דירה לא בא להתרשם — הוא בא למדוד. הוא רוצה לדעת אם
המחיר הזה סביר, אם הקילומטראז' חריג, אם הדירה יקרה לאזור שלה. לוח
מודעות שמעוצב כחנות עונה על השאלה הלא נכונה.

מכאן נגזר הכול: גרפיט כשלדה, עצם כפני החוגה, וענבר זרחני של מד דלק
כצבע הקריאה היחיד. תוצאות מוצגות כשורות קריאה ולא כגריד כרטיסים, וכל
מודעה נושאת סקאלה עם מחוג. **מדד המחיר אינו רכיב בדף — הוא הדקדוק של
האתר כולו**, וזה גם ההבדל המהותי מול כל לוח אחר.

הבחירה הזו מוותרת במודע על משהו: הכיוון קר, ומוכר פרטי שמעלה ספה עלול
להרגיש שהאתר לא בשבילו. זה מחיר ידוע ולא תופעת לוואי.

---

## פלטה

הצבעים מוגדרים כ-HSL ב-`globals.css` כדי ש-Tailwind יוכל לגזור שקיפויות
(`bg-primary/10`).

| טוקן | חוגה בהירה | חוגה כהה | תפקיד |
| --- | --- | --- | --- |
| `--ink` | `#16181B` | `#E6E2D6` | טקסט ראשי |
| `--bone` | `#E6E2D6` | `#16181B` | פני החוגה — רקע הדף |
| `--amber` | `#8A5200` | `#FFB000` | **צבע הפעולה היחיד** |
| `--verdict` | `#10606E` | `#58C6D8` | פסק דין. מחיר מתחת לחציון, מידע. |
| `--hair` | `#C9C3B4` | `#32363C` | גבולות, מפרידים, שלד טעינה |
| `--alarm` | `#B33A12` | `#E8734A` | אזהרות וחריגה כלפי מעלה. **שום דבר אחר.** |

### חוק ברזל: צבע פעולה אחד

אם במסך אחד יש שני כפתורים צבעוניים — אחד מהם טועה. פעולה משנית היא
כפתור רפאים או קישור, לא צבע שני. `--verdict` הוא צבע קריאה ואין לו
כפתור מלא בשום מסך.

### שתי חוגות, לא היפוך

הערכה הכהה היא **הקנונית** של הכיוון; הבהירה נבנתה מחדש ואינה היפוך
שלה. ההבדל המהותי הוא בענבר: `#FFB000` על גרפיט נותן יחס 9.7, ועל עצם
בהיר הוא נותן 1.4 — כלומר כפתור שאי אפשר לקרוא. לכן בחוגה הבהירה הענבר
מעומק לברונזה `#8A5200`, והענבר הזוהר נשאר בסקאלה בלבד.

### קבועים שאינם תלויים בערכה

`--scale-plate`, `--scale-hair` ו-`--needle` זהים בשתי הערכות.

לחוגה של מכשיר אמיתי יש פנים כהות בלי קשר לתאורה בחדר, וזה גם מה
שמאפשר למחוג לשמור על אותה עוצמה בשתי הערכות. **אלה אינם צבעי ממשק
ואסור להשתמש בהם מחוץ לסקאלה.**

---

## סקאלת המחיר

אלמנט החתימה. לוחית גרפיט עם שנתות ומחוג ענבר שמסמן את האחוזון שבו
יושב המחיר בתוך המודעות הדומות.

- השנתות מצוירות בשני גרדיאנטים חוזרים ולא ב-20 אלמנטים. בדף עם 40
  מודעות זה ההבדל בין 40 צמתי DOM ל-800.
- המחוג ממוקם ב-`inset-inline-start`, כלומר הקצה הזול יושב בצד שממנו
  מתחילים לקרוא.
- בגריד הסקאלה יושבת מתחת למחיר; בשורת קריאה היא עוברת לעמודה משלה
  בקצה השורה, וברוחב מכל קטן מ-34rem היא נדחקת לשורה משלה מתחתיה.
  היא **לא נעלמת** במסך צר — היא הסיבה שהמשתמש כאן.

**מתחת ל-8 מודעות להשוואה לא מוצג דבר.** לא מחוג חיוור ולא הערכה
זהירה. סטייה מתחת ל-3% מהחציון גם היא לא מוצגת — היא רעש סטטיסטי,
ומחוג שזז באחוז אחד מלמד את המשתמש להתעלם ממנו.

---

## טיפוגרפיה

שלושה גופנים, שלושה תפקידים. הטעינה ב-`layout.tsx`.

| תפקיד | גופן | משקל / רוחב | שימוש |
| --- | --- | --- | --- |
| Display | Noto Sans Hebrew | 700–800 · `wdth` 75% | כותרות מסך, מחיר ראשי, מספרים בסטטיסטיקות |
| Body | Assistant | 400 / 600 | כל טקסט הממשק |
| Data | Rubik + `tabular-nums` | 400 / 500 / 700 | מחיר, ק"מ, מ"ר, שנה, טלפון |

### למה גרוטסק מעובה־צר ולא סריף

הכותרות היו קודם בפרנק רוהל ליברה. סריף נותן אופי — אבל האופי שהוא
נותן הוא של עיתון או של ספר, לא של מכשיר מדידה. הכיוון קורא לצורה
שנקראת כמו הדפסה על לוחית: צרה, כבדה, בלי תגים.

**`Noto Sans Hebrew` הוא הגופן העברי היחיד ב-Google Fonts עם ציר רוחב
(`wdth`) אמיתי.** נבדק מול ה-API: הבבו, רוביק, אלף, קרנטינה, סקולר וואן
וסואץ וואן קיימים — אך אף אחד מהם אינו נותן צרות. "צר" שמזויף ב-
`transform: scaleX()` שובר את יחסי הקווים של האות ונראה בדיוק כמו מה
שהוא.

**`font-stretch: 75%` הוא מה שמפעיל את הציר.** בלעדיו נטען גופן עם ציר
`wdth` שמוצג ברוחב 100% — כלומר בדיוק כמו כל גרוטסק אחר, והכיוון כולו
נשען על הצרות. אימות שהציר אכן פועל: אותה מחרוזת נמדדה 411px ברוחב
100% מול 372px ב-82%. אילו הציר היה מתעלם, שתי המדידות היו זהות.

הערך נבחר אחרי שהעמדנו שורה זהה ב-100 / 90 / 82 / 75 / 68 וקראנו אותה.
ב-68% החללים הפנימיים של מ', ס' ו-ם מתחילים להצטמצם, ולעברית אין
אורכי אותיות מעל ומתחת לשורה שייצרו מקצב לפצות על כך — הכותרת הופכת
לפס אחיד בגדלים קטנים. ב-75% הצורה עדיין פתוחה.

**`letter-spacing` חיובי קל ולא שלילי.** בלטינית מכווצים גם את המרווח,
אבל בעברית אין אותיות גדולות שיוצרות מקצב, וכיווץ כפול — צורה ומרווח —
הופך שורת כותרת למקטע אחד רצוף.

**Data עם `tabular-nums`** — ספרות ברוחב קבוע, כדי שעמודת מחירים בגריד
תתיישר לאורך השורות במקום לרקוד.

### סקאלה סגורה

```
12 / 14 / 16 / 20 / 26 / 34 / 46
```

אין גדלים אחרים. `text-[15px]` בקוד הוא באג. הסקאלה מוגדרת ב-`fontSize`
של `tailwind.config.ts` ומחליפה את ברירת המחדל של Tailwind, כך שגודל
מחוץ לסקאלה פשוט לא קיים כמחלקה.

הבחנה בין שורות שאינה בגודל נעשית **בצבע**: שורת המפרט בכרטיס ושורת
המיקום שתחתיה שתיהן 12px, ונבדלות ב-`--ink/60` מול `--ink/45`. זה שומר
על הסקאלה ועדיין יוצר היררכיה.

### מספרים בתוך טקסט עברי

כל מספר עטוף ב-`.num`, שמחיל `direction: ltr` + `unicode-bidi: isolate`.
בלעדיהם `₪1,234` מוצג הפוך בתוך משפט עברי.

---

## פורמט מספרים

הכלל חי בקוד ב-`src/lib/format.ts`, שהוא **המקור היחיד** לפורמט מספרים.
שום קומפוננטה לא קוראת ל-`toLocaleString` בעצמה.

מספרים נחלקים לשתי משפחות שנראות זהות בקוד ומתנהגות הפוך על המסך:

| משפחה | דוגמה | מפריד אלפים |
| --- | --- | --- |
| **מדידה** — כמה | `84,000 ק"מ`, `₪27,000` | כן |
| **מזהה** — איזה | `2016`, `קומה 3`, `יד 2` | **לא** |

הבאג שהוליד את הכלל: `BYD סיל 2,016`.

`npm run check:format` מקבע את הכללים.

---

## גריד וצפיפות

- **בסיס 4px** לכל מרווח.
- **רדיוס: אפס.** מכשיר מדידה אינו מעוגל, והפינה החדה היא מה שמבדיל את
  הלוח מכל כרטיס `rounded-lg` באינטרנט. `rounded-full` אינו נגזר
  מ---radius ונשאר זמין לצ'יפים ולאווטרים בלבד.

הערכים ב-`borderRadius` כתובים מפורשות ולא כ-`calc(var(--radius) - 4px)`:
חיסור מאפס נותן רדיוס שלילי, שהוא הצהרה לא חוקית שהדפדפן זורק בשקט.
התוצאה במקרה הזה זהה, אבל CSS שנשען על כך שהדפדפן יתעלם ממנו הוא באג
שממתין לשינוי הבא ב---radius.

### אין צללים

במכשיר מדידה משטח אינו מרחף מעל משטח אחר — הוא נחתך ממנו. ההפרדה היא
קו, לא עומק מדומה.

`shadow-lifted` נשאר בשם הזה כי הוא בשימוש בעשרות מקומות, אבל הוא כבר
אינו צל אלא טבעת שיער בעובי פיקסל. ההיענות של כרטיס ל-hover היא **הארה
של המשטח** (`hover:bg-secondary`), כמו שורה שנבחרת במכשיר.

**החריג היחיד** הוא `shadow-overlay`, לשכבות פורטל בלבד — תפריט, דיאלוג,
טולטיפ. הן אינן יושבות בזרימת הדף ואין להן קו שמפריד אותן ממנה, ובלי
הצללה הן נקראות כחלק מהתוכן שמתחתיהן. אסור בכרטיס, בשורה או במשטח
בזרימה.

### שני מצבי צפיפות

המשתמש בוחר, והבחירה נשמרת ב-`localStorage` (`src/stores/density.ts`).

| מצב | תיאור |
| --- | --- |
| `grid` | תמונה גדולה, שתי עמודות בנייד. עיון. |
| `list` | **שורת קריאה** — תמונה מימין, תוכן, וסקאלה בעמודה בקצה. |

מצב `list` אינו נוחות — הוא מה שסוחרי רכב ומחפשי דירות עושים בפועל,
והוא הצורה שבה הכיוון הזה מתבטא במלואו.

**עמודת הסקאלה מרונדרת תמיד**, גם כשאין קריאה. עמודה שנעלמת מזיזה את
המחיר של אותה שורה ושוברת את יישור העמודה לאורך הרשימה — ובמכשיר זה
חמור יותר מחוסר הנתון עצמו. במקום להיעלם, היעדר הקריאה נאמר במילים:
`אין מספיק מודעות דומות להשוואה`, או `במחיר החציון` כשהסטייה זניחה.

המחיר עטוף ב-`<span class="num">` ולא נושא את המחלקה על ה-`<p>`:
`.num` מחיל `direction: ltr`, ועל הפסקה הוא גורר גם את היישור — המחיר
נצמד לשמאל בעוד הכותרת שמתחתיו נצמדת לימין.

---

## נרמול תמונות

כל תמונה עוברת דרך `ListingImage`, בלי יוצא מן הכלל. זה הכלל בעל
ההשפעה הגדולה ביותר על איך הלוח נראה.

1. **יחס 4:3 קבוע** עם `object-fit: cover`. אף פעם לא letterbox, אף פעם
   לא מלבן ריק. גובה קבוע הוא גם מה שמחזיק CLS על 0.
2. **`blurhash` כ-placeholder** — הטעינה מתחילה מהצבע האמיתי של התמונה
   ולא ממלבן אפור.
3. **שכבת scrim** (`.img-scrim`) מתחת לתגיות שיושבות על התמונה. אי אפשר
   לדעת אם פינת התמונה בהירה או כהה, וה-scrim מבטיח קריאוּת בלי לצבוע
   את כל התמונה.
4. **תמונה חסרה** → קווי המתאר של הקטגוריה ב-`--stone`. לא אייקון
   "תמונה שבורה".

---

## כרטיס המודעה

```
[תמונה 4:3 · תג "מקודם" בפינה · לב במיקום קבוע]
₪27,000                        Rubik tabular, 20px, --ink — הכי בולט
[מד המחיר]                     פס 2px, מוצג רק כשיש בסיס להשוואה
BYD סיל 2016                   Assistant 600, 16px, שורה אחת + ellipsis
2016 · יד שנייה · 84,000 ק"מ    3 שדות בלבד, 12px, --ink/60
רמת השרון · לפני יומיים         12px, --ink/45
```

### מה הוסר מהכרטיס במכוון

**דירוג המוכר, ספירת צפיות, מספר התמונות.** שלושתם רעש ברשימה: הם לא
עוזרים לבחור בין מודעות, והם מה שהופך לוח לצפוף. מקומם בדף המודעה,
אחרי שהמשתמש כבר נכנס.

### כלל גלובלי: אין שדה בלי תווית

ערך מספרי עירום אסור להופיע בכרטיס. `₪120,000` לבד הוא מספר; `מחזור
חודשי ₪120,000` הוא מידע. הכלל נאכף בקומפוננטת הכרטיס דרך
`formatAttributeEntry`, שמסמן אילו ערכים מסבירים את עצמם (`יד שנייה`,
`84,000 ק"מ`) ואילו חייבים תווית.

### תג "מקודם"

קטן, בצבע טקסט משני, על רקע כרטיס חצי-שקוף. לא צהוב ולא בולט.
**מודעה מקודמת מנצחת במיקום ברשימה, לא בצעקנות.**

---

## מד המחיר — אלמנט החתימה

הקוד: `src/lib/price-meter.ts`, `src/components/listing/price-meter.tsx`.

בלוח מודעות המחיר לבדו חסר משמעות. `₪27,000` הוא זול או יקר רק ביחס
לרכבים דומים — וזה בדיוק המידע שקונה מחפש ואין לו בשום לוח בישראל.

פס דק ברוחב מלא מתחת למחיר, עם סמן במיקום האחוזון, ומתחתיו
`12% מתחת לחציון` ב-`--sign` או `9% מעל החציון` ב-`--ink/60`.

**כללים שאין לעקוף:**

- פחות מ-8 מודעות להשוואה → הפס **לא מוצג כלל**. עדיף בלי נתון מאשר
  נתון שמומצא משלוש מודעות.
- סטייה של פחות מ-3% מהחציון היא רעש סטטיסטי ולא "מחיר טוב" — גם אז
  לא מוצג כלום.
- החישוב ב-SQL בשאילתה אחת לכל עמוד, לא אחת לכרטיס.

"דומה" = אותה תת-קטגוריה, ובקטגוריות שיש בהן ערך השוואה גם טווח קרוב
שלו: ±3 שנות ייצור ברכב, ±חדר בנדל"ן. הערך מנורמל לעמודה
`Listing.comparableBand` עם אינדקס ייעודי, כדי שההשוואה לא תדרוש
`join` אל טבלת השדות הדינמיים בכל שאילתה.

---

## התנהגות שנגזרת מהתוכן

ארבעה מנגנונים שנוספו כדי שהממשק יגיב לתוכן ולמשתמש, בלי להוסיף
ולו צבע אחד למסגרת.

| מנגנון | איפה | מה הוא עושה |
| --- | --- | --- |
| `container-type` על הכרטיס | `.listing-card` | נקודות השבירה נמדדות מול המכל ולא מול החלון. אותו רכיב בגריד, בסיידבר וברצועה — בלי prop |
| צבע נגזר מהמודעה | `src/lib/listing-accent.ts` | הילה מאחורי הגלריה וקו מתחת למחיר, בגוון הממוצע של התמונה |
| צפיפות נלמדת | `src/stores/density.ts` | שלוש בחירות רצופות בקטגוריה הופכות לברירת המחדל שם |
| ספירה בכל פילטר | `countByAttributeValues` | "מעלית (31)" לפני הלחיצה. פילטר לא סופר את עצמו |

### הצבע הנגזר — הגבולות

הצבע נוגע ב**שני מקומות בדף המודעה בלבד**, ושניהם מרוסנים: רוויה
בין 14% ל-42% ובהירות בין 34% ל-56%. תמונה אפורה לא מקבלת צבע כלל.
ירוק השילוט נשאר צבע הפעולה היחיד — הצבע הנגזר אינו כפתור, אינו
קישור ואינו מצב.

### מספרים אף פעם לא נחתכים

`.num` מבודד מספר ל-LTR, וחיתוך בקצה שורה ב-RTL אוכל את הספרות
**המובילות**: `417,851 ק"מ` הופך ל-`7,851 ק"מ` — מספר שגוי שנראה
תקין. לכן ערך שיש בו ספרה לעולם אינו מתכווץ; הטקסט שלצדו מתקצר
במקומו, ושדה שעדיין לא נכנס יורד כולו.

---

## תנועה

**תקציב הדוק:** מקסימום 180ms, easing `cubic-bezier(.2,0,0,1)`.
שניהם ברירת המחדל ב-`tailwind.config.ts`, כך שקשה לחרוג מהם בטעות.

תנועה מותרת בשישה מקומות בלבד:

1. פתיחת סדין הפילטרים
2. hover על כרטיס (הצל בלבד)
3. lightbox של הגלריה
4. מעבר skeleton→תוכן (fade 120ms)
5. מעבר תמונת המודעה בין הכרטיס לדף המודעה
6. פרישת ההיסטוגרמה בדף המודעה

**אפס** parallax. **אפס** טקסט אות-אות. זה לוח מודעות; אנשים באים
לעבוד.

**חריג יחיד ל-scroll-reveal:** ההיסטוגרמה בדף המודעה נפרסת בגלילה
(`animation-timeline: view()`, בלי JavaScript). ההבדל מקישוט הוא
שהתנועה כאן היא הנתון עצמו — כל עמודה עולה לגובה שמייצג את מספר
המודעות בתא. שום אלמנט אחר באתר אינו מונפש בגלילה.

**מעבר בין דפים** נוסף לרשימה: תמונת המודעה עוברת מהכרטיס לדף המודעה
דרך View Transitions API (`src/components/view-transition-link.tsx`).
אותו תקציב זמן, ואותו easing.

`prefers-reduced-motion` מבטל הכול חוץ מ-opacity.

---

## כתיבה בממשק

- **כפתור אומר מה יקרה:** `פרסם מודעה`, לא `המשך`. הטוסט אחריו:
  `המודעה פורסמה`.
- **שגיאה מסבירה מה לעשות:** `המחיר גבוה מהמותר בקטגוריה. הזן עד
  ₪5,000,000.` לא `שגיאה בשדה`.
- **מצב ריק אומר מה להסיר:** `אין תוצאות. נסה בלי "מעלית" — 31 מודעות
  מתאימות.` עם כפתור שמסיר את הפילטר.
- אפס סימני קריאה. אפס אמוג'ים בממשק. אפס "אופס!".

---

## צבעים מחוץ ל-CSS

ארבעה הקשרים לא מריצים CSS ולכן לא יכולים לקרוא טוקן: `next/og` (Satori),
מניפסט ה-PWA, תבניות הדוא"ל, ומאפייני `paint` של MapLibre.

כולם קוראים מ-`src/lib/palette.ts`. **הערכים שם חייבים להישאר תואמים
ל-`globals.css`** — שינוי בצד אחד בלי הצד השני הוא באג.

---

## RTL

- מאפיינים לוגיים בלבד: `ms-`/`me-`/`start`/`end`. לא `ml-`/`mr-`.
- חצי ניווט הפוכים (חץ "הבא" מצביע שמאלה).
- `<Slider dir="rtl">` — Radix לא יורש כיוון מהמסמך.
- גרפי Recharts עטופים ב-`dir="ltr"` — הספרייה מניחה צירים משמאל לימין.
- מספרים ב-`.num`, ראה למעלה.
