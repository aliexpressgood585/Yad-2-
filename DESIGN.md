# לוח — מערכת העיצוב

מסמך זה הוא המקור לכללי העיצוב. כל טוקן כאן מוגדר ב-`src/app/globals.css`
או ב-`tailwind.config.ts`, וכל חריגה ממנו בקוד היא באג ולא בחירה.

---

## הבעיה שהעיצוב פותר

לוחות מודעות נראים רע מסיבה מבנית, לא מסיבת טעם. הם מציגים כמות עצומה
של תוכן שהועלה בידי משתמשים — תמונות באיכות, ביחס ובבהירות אקראיים —
ומנסים לפצות על חוסר האחידות בעוד צבע, עוד מסגרת, עוד תג ועוד באנר.
כל תוספת כזו מוסיפה רעש, והרעש מצטבר.

הכיוון ההפוך הוא הפתרון: **מסגרת ויזואלית קפדנית שגורמת גם לתמונה בינונית
להיראות מכובדת.** ככל שהמסגרת אחידה יותר, כך התוכן שבתוכה נסבל יותר.

מכאן נגזרים כל הכללים הבאים. הם מגבילים בכוונה.

---

## מקור ההשראה

**מכשיר מדידה.**

הלוח אינו חנות ואינו מגזין. הוא כלי שאדם פותח כדי לברר מה שווה משהו,
ולכן הוא מעוצב כמד — לוח קריאה גרפיטי, ספרות במונו-רווח, מחוג אחד
בענבר זרחני, ואפס קישוט. כל בחירה למטה נגזרת מהמשפט הזה.

מכאן גם הכלל שקודם לכל השאר:

> **מכשיר שמראה קריאה שאין לו הוא מכשיר מקולקל.**

מתחת לשמונה מודעות דומות אין מחוג. לא מחוג חלש, לא מחוג אפור, לא הערה
באותיות קטנות. אין.

---

## פלטה

חמישה צבעים. הצבעים מוגדרים כ-HSL ב-`globals.css` כדי ש-Tailwind יוכל
לגזור שקיפויות (`bg-primary/10`), ובמקביל כערכים מוחלטים ב-`palette.ts`
עבור ארבעת ההקשרים שלא מריצים CSS. `npm run check:design` נופל אם השניים
מתפצלים.

### פנים המכשיר — ברירת המחדל

| טוקן | ערך | תפקיד |
| --- | --- | --- |
| `--graphite` | `#16181B` | קרקע. לוח המכשיר. |
| `--chassis` | `#1E2126` | משטח. שלדה שמורמת מהקרקע בהפרש ערך, לא בצל. |
| `--bone` | `#E6E2D6` | טקסט. עצם, לא לבן — לבן טהור על גרפיט זוהר. |
| `--amber` | `#FFB000` | **צבע הקריאה היחיד.** מחוג, פעולה, פוקוס. |
| `--cyan` | `#58C6D8` | **פסק דין בלבד** — "מתחת לחציון". |
| `--rule` | `#2C3037` | קווי מסגרת ומפרידים. |

### חוק ברזל: כפתור מלא אחד לכל מסך

אם במסך אחד יש שני כפתורים מלאים — אחד מהם טועה. פעולה משנית היא כפתור
מתאר או קישור, לא צבע שני.

**ההדר אינו נושא את הענבר המלא.** הוא רהיט קבוע, והפעולה המלאה שייכת
למסך שנמצאים בו: "הצגת מספר הטלפון" בדף מודעה, "חיפוש" בדף הבית,
"פרסום מודעה" באשף הפרסום. אילו ההדר היה מלא, כל מסך באתר היה מציג שני
כפתורי ענבר.

**הציאן אינו פעולה.** אין לו כפתור מלא בשום מסך. הוא אומר דבר אחד:
המחיר הזה מתחת לחציון.

### פנים היום

פנים היום **נבנתה מחדש ואינה היפוך.** היפוך היה משאיר את ענבר #FFB000
על קרקע עצם, יחס 1.9:1 — טקסט בלתי קריא. במקום זה הענבר נבנה מחדש
כענבר שרוף `#7A4A00` (5.8:1), והציאן כ-`#0E5F6E` (5.6:1). התפקידים
זהים, הערכים אינם.

פנים המכשיר היא ברירת המחדל ואינה נגזרת מהעדפת מערכת ההפעלה — ראה
`DECISIONS.md` §37.

---

## טיפוגרפיה

שלושה גופנים, שלושה תפקידים. הטעינה ב-`layout.tsx`.

| תפקיד | גופן | משקלים | שימוש |
| --- | --- | --- | --- |
| Heading | Heebo | 700 / 900 | כותרות מסך והמחיר הראשי בדף מודעה |
| Body | Assistant | 400 / 600 | כל טקסט הממשק |
| Data | IBM Plex Mono + `tabular-nums` | 400 / 500 / 600 | כל מספר שנקרא כמדידה |

**כותרות בגרוטסק עברי מעובה-צר.** אין בעברית גרוטסק חופשי עם ציר רוחב
(`font-stretch`), ולכן ה"צר" מושג במשקל 900 יחד עם `letter-spacing:
-0.02em`. מעבר לערך הזה אותיות עבריות סמוכות מתחילות להתחבר.

**מספרים במונו-רווח אמיתי.** זה לא נוי: במכשיר מדידה העין משווה *מיקום
ספרה* ולא ערך מספרי, וטור מחירים שאינו מתיישר לאורך השורות אי אפשר
לסרוק. גופן פרופורציונלי עם ספרות טבלאיות מיישר רוחב בלבד; מונו מיישר
הכול. ל-Plex Mono אין עברית, וערך עברי בתוך `.num` ("יד שנייה") נופל
אחורה ל-Assistant דרך שרשרת ה-fallback — וזה בסדר, כי מה שנחוץ כאן הוא
הספרות.

### סקאלה סגורה

```
12 / 14 / 16 / 20 / 26 / 34 / 46
```

אין גדלים אחרים. `text-[15px]` בקוד הוא באג. הסקאלה מוגדרת ב-`fontSize`
של `tailwind.config.ts` ומחליפה את ברירת המחדל של Tailwind, כך שגודל
מחוץ לסקאלה פשוט לא קיים כמחלקה.

הבחנה בין שורות שאינה בגודל נעשית **בצבע**: שורת המפרט בשורת התוצאה
ושורת המיקום שתחתיה שתיהן 12px, ונבדלות ב-`--muted-foreground` מול
`--muted-foreground/75`.

### מספרים בתוך טקסט עברי

כל מספר עטוף ב-`.num`, שמחיל `direction: ltr` + `unicode-bidi: isolate`.
בלעדיהם `₪1,234` מוצג הפוך בתוך משפט עברי.

---

## פורמט מספרים

הכלל חי בקוד ב-`src/lib/format.ts`, שהוא **המקור היחיד** לפורמט מספרים.
שום קומפוננטה לא קוראת ל-`toLocaleString` בעצמה.

מספרים נחלקים לשתי משפחות שנראות זהות בקוד ומתנהגות הפוך על המסך:

| משפחה | דוגמה | מפריד אלפים |
| --- | --- | --- |
| **מדידה** — כמה | `84,000 ק"מ`, `₪27,000` | כן |
| **מזהה** — איזה | `2016`, `קומה 3`, `יד 2` | **לא** |

הבאג שהוליד את הכלל: `BYD סיל 2,016`.

`npm run check:format` מקבע את הכללים.

---

## גריד וצפיפות

- **בסיס 4px** לכל מרווח.
- **רדיוס: אפס.** בכל מקום, כולל צ'יפים ואווטרים.
- **צל: אפס.** בכל מקום, כולל שכבות פורטל.

### אפס פינות ואפס צללים — ואיך זה נאכף

פינה מעוגלת היא רמז לתוכנה ידידותית, וצל הוא סימולציה של עומק פיזי.
המכשיר אינו מנסה להיות אף אחד מהשניים. הפרדה בין משטחים נעשית בקו של
פיקסל אחד ובהפרש הערך בין גרפיט לשלדה — שני אמצעים שאינם מטשטשים דבר.

האכיפה אינה בסקירת קוד אלא בטוקנים: **כל** סקאלת `borderRadius`
מצביעה על `--radius` שהוא `0px`, ו**כל** סקאלת `boxShadow` היא `none`.
לכן `rounded-full` או `shadow-lg` שנכתבים ברכיב חדש פשוט אינם עושים
דבר. `npm run check:design` נופל אם ערך בסקאלות האלה חדל להיות אפס, וגם
אם `box-shadow` גולמי חוזר ל-`globals.css`.

### שני מצבי צפיפות

המשתמש בוחר, והבחירה נשמרת ב-`localStorage` (`src/stores/density.ts`).
שני המצבים הם שורות ברוחב מלא; **גריד כרטיסים אינו קיים באתר**
(`DECISIONS.md` §38).

| מצב | תיאור |
| --- | --- |
| `full` | לוחית תמונה של 11rem. עיון בפריטים שהמראה שלהם הוא הנתון. |
| `compact` | חתימת תמונה של 6rem, שורות צמודות. סריקת טור מחירים. |

מצב `compact` אינו נוחות — הוא מה שסוחרי רכב ומחפשי דירות עושים בפועל.

---

## נרמול תמונות

כל תמונה עוברת דרך `ListingImage`, בלי יוצא מן הכלל. זה הכלל בעל
ההשפעה הגדולה ביותר על איך הלוח נראה.

1. **יחס 4:3 קבוע** עם `object-fit: cover`. אף פעם לא letterbox, אף פעם
   לא מלבן ריק. גובה קבוע הוא גם מה שמחזיק CLS על 0.
2. **`blurhash` כ-placeholder** — הטעינה מתחילה מהצבע האמיתי של התמונה
   ולא ממלבן אפור.
3. **שכבת scrim** (`.img-scrim`) מתחת לתגיות שיושבות על התמונה. אי אפשר
   לדעת אם פינת התמונה בהירה או כהה, וה-scrim מבטיח קריאוּת בלי לצבוע
   את כל התמונה.
4. **תמונה חסרה** → קווי המתאר של הקטגוריה ב-`--rule`. לא אייקון
   "תמונה שבורה".

---

## שורת התוצאה

```
┌──────────────────────────────────────────────────────────────────┐
│                    │ דירת גן 4 חדרים ברמת גן │ ₪3,810,000        │
│  [תמונה 4:3]       │ 4 חד' · 108 מ"ר · יש    │ ├┼┼┼┬┼┼┼┼┤        │
│  [תג "מקודם"]      │ רמת גן · לפני יומיים    │ 62% מעל החציון    │
└──────────────────────────────────────────────────────────────────┘
```

מסגרת השורה וההילה שמתחתיה צבועות בצבע הנגזר מהתמונה של אותה מודעה
(ראה למטה). כל השאר בפלטה.

**הטור בצד הוא העיקר.** המחיר והסקאלה יושבים בטור קבוע ברוחב 13rem
המופרד בקו, ולכן המחירים של כל התוצאות מתיישרים לאורך קו אנכי אחד
במונו-רווח. זה מה שגריד כרטיסים אינו מאפשר, וזו הסיבה שהוא ירד מהאתר
(`DECISIONS.md` §38).

במכל צר מ-34rem הטור יורד מתחת לגוף השורה. נקודות השבירה נמדדות מול
**המכל** ולא מול החלון (`@container row`), ולכן אותו רכיב עובד ברשימה,
בסיידבר, ברצועת "נצפו לאחרונה" ובחלונית של המפה בלי אף prop.

### מה הוסר מהשורה במכוון

**דירוג המוכר, ספירת צפיות, מספר התמונות.** שלושתם רעש ברשימה: הם לא
עוזרים לבחור בין מודעות, והם מה שהופך לוח לצפוף. מקומם בדף המודעה,
אחרי שהמשתמש כבר נכנס.

### כלל גלובלי: אין שדה בלי תווית

ערך מספרי עירום אסור להופיע בשורה. `₪120,000` לבד הוא מספר; `מחזור
חודשי ₪120,000` הוא מידע. הכלל נאכף דרך `formatAttributeEntry`,
שמסמן אילו ערכים מסבירים את עצמם (`יד שנייה`, `84,000 ק"מ`) ואילו
חייבים תווית.

### שדה שאינו נכנס יורד כולו

`.num` מבודד מספר ל-LTR, וחיתוך בקצה שורה ב-RTL אוכל דווקא את הספרות
**המובילות**: `417,851 ק"מ` הופך ל-`7,851 ק"מ` — מספר שגוי שנראה תקין
לחלוטין. לכן ערך שיש בו ספרה לעולם אינו מתכווץ; הטקסט שלצדו מתקצר
במקומו, ושדה שעדיין לא נכנס יורד כולו.

### תג "מקודם"

קטן, בצבע טקסט משני, על רקע שלדה. לא ענבר ולא בולט.
**מודעה מקודמת מנצחת במיקום ברשימה, לא בצעקנות.**

---

## הסקאלה — הדקדוק של האתר

הקוד: `src/lib/price-meter.ts`, `src/components/listing/price-gauge.tsx`.

בלוח מודעות המחיר לבדו חסר משמעות. `₪27,000` הוא זול או יקר רק ביחס
לרכבים דומים — וזה בדיוק המידע שקונה מחפש.

**מדד המחיר אינו רכיב אלא הדקדוק של האתר.** אותה צורה בדיוק, באותו מקום
ביחס למחיר, בכל מסך שמוצג בו מחיר: פס עם שנתות כל 10%, שנתת חציון ארוכה
במרכז, ומחוג ענבר במיקום האחוזון. מתחתיו המסקנה בציאן (`מתחת לחציון`)
או בטקסט משני (`מעל החציון`).

**כללים שאין לעקוף:**

- **פחות מ-8 מודעות להשוואה → אין סקאלה בכלל.** לא סקאלה חלשה, לא מחוג
  אפור, לא "מדגם קטן" באותיות קטנות. הכלל נאכף בשני מקומות —
  ב-`priceMetersFor` (`HAVING count(*) >= 8`) וברכיב עצמו — ושניהם
  נבדקים ב-`npm run check:design`.
- **המחוג מוצג תמיד כשיש קריאה.** מיקומו *הוא* הנתון. מה שמותנה בסף
  3% הוא רק המסקנה המילולית: סטייה של אחוז מהחציון היא רעש סטטיסטי,
  ולכתוב "1% מתחת לחציון" זה להפוך רעש לטענה. ראה `DECISIONS.md` §39.
- **החישוב ב-SQL בשאילתה אחת לכל עמוד**, לא אחת לשורה.
- **השנתות מצוירות ב-`repeating-linear-gradient`** ולא באלמנטים. חמישה
  `<span>` לכל שורה הם 120 אלמנטים נוספים בעמוד של 24 מודעות.

"דומה" = אותה תת-קטגוריה, ובקטגוריות שיש בהן ערך השוואה גם טווח קרוב
שלו: ±3 שנות ייצור ברכב, ±חדר בנדל"ן. הערך מנורמל לעמודה
`Listing.comparableBand` עם אינדקס ייעודי, כדי שההשוואה לא תדרוש
`join` אל טבלת השדות הדינמיים בכל שאילתה.

---

## התנהגות שנגזרת מהתוכן

ארבעה מנגנונים שגורמים לממשק להגיב לתוכן ולמשתמש, בלי להוסיף ולו צבע
אחד למסגרת.

| מנגנון | איפה | מה הוא עושה |
| --- | --- | --- |
| `container-type` על השורה | `.listing-row` | נקודות השבירה נמדדות מול המכל ולא מול החלון. אותו רכיב ברשימה, בסיידבר וברצועה — בלי prop |
| צבע נגזר מהמודעה | `src/lib/listing-accent.ts` | מסגרת השורה וההילה שמתחתיה, בגוון הדומיננטי של התמונה |
| צפיפות נלמדת | `src/stores/density.ts` | שלוש בחירות רצופות בקטגוריה הופכות לברירת המחדל שם |
| ספירה בכל פילטר | `countByAttributeValues` | "מעלית (31)" לפני הלחיצה. פילטר לא סופר את עצמו |

### שכבת הקרינה — הצבע הנגזר

מקור הצבע הוא ה-blurhash שכבר שמור על כל תמונה: ארבעת התווים הראשונים
אחרי הכותרת מקודדים את הצבע הממוצע (רכיב ה-DC). אין קריאת רשת, אין
קנבס, אין עיבוד תמונה.

**הצבע נוגע בשני מקומות בלבד: מסגרת השורה וההילה שמתחתיה.** הוא אינו
כפתור, אינו קישור ואינו מצב. הענבר נשאר צבע הפעולה היחיד.

שני שומרים, ושניהם חובה:

**רצפת רוויה** — 22% עד 55%. מתחת ל-8% אין צבע כלל (תמונה אפורה אינה
מקבלת רוויה מלאכותית). הרצפה עלתה מ-14% של הסבב הקודם כי הצבע יושב
עכשיו על קו ברוחב פיקסל אחד ולא בהילה מטושטשת, וקו ברוויה 14% נקרא
כאפור.

**בדיקת ניגודיות מול הרקע** — יחס 3.0 לפחות, הסף של WCAG לרכיב ממשק
שאינו טקסט. הבהירות מחושבת **פעמיים, פעם לכל קרקע**, כי אותו ערך אינו
יכול לעבור גם מול גרפיט וגם מול עצם. בלי הבדיקה גוון כחול כהה היה נעלם
על גרפיט וגוון צהוב על עצם, והמשתמש היה רואה שורות שחלקן ממוסגרות
וחלקן לא — כלומר באג, ולא כלל.

הבהירות נכפית לרצועה **לפני** הבדיקה (45%–68% על גרפיט, 26%–46% על
עצם): הצבע הממוצע של תמונת מוצר נוטה להיות בהיר מאוד, ובהירות 93%
עוברת ניגודיות בקלות אבל היא כמעט לבן.

`npm run check:accent` מקבע את שני השומרים. הנימוק המלא ב-`DECISIONS.md`
§40.

### מספרים אף פעם לא נחתכים

`.num` מבודד מספר ל-LTR, וחיתוך בקצה שורה ב-RTL אוכל את הספרות
**המובילות**: `417,851 ק"מ` הופך ל-`7,851 ק"מ` — מספר שגוי שנראה
תקין. לכן ערך שיש בו ספרה לעולם אינו מתכווץ; הטקסט שלצדו מתקצר
במקומו, ושדה שעדיין לא נכנס יורד כולו.

---

## תנועה

**תקציב הדוק:** מקסימום 180ms, easing `cubic-bezier(.2,0,0,1)`.
שניהם ברירת המחדל ב-`tailwind.config.ts`, כך שקשה לחרוג מהם בטעות.

תנועה מותרת בשישה מקומות בלבד:

1. פתיחת סדין הפילטרים
2. hover על שורת תוצאה (צבע המסגרת בלבד)
3. lightbox של הגלריה
4. מעבר skeleton→תוכן (fade 120ms)
5. מעבר תמונת המודעה בין השורה לדף המודעה
6. פרישת ההיסטוגרמה בדף המודעה

**אפס** parallax. **אפס** טקסט אות-אות. זה לוח מודעות; אנשים באים
לעבוד.

**חריג יחיד ל-scroll-reveal:** ההיסטוגרמה בדף המודעה נפרסת בגלילה
(`animation-timeline: view()`, בלי JavaScript). ההבדל מקישוט הוא
שהתנועה כאן היא הנתון עצמו — כל עמודה עולה לגובה שמייצג את מספר
המודעות בתא. שום אלמנט אחר באתר אינו מונפש בגלילה.

**מעבר בין דפים** נוסף לרשימה: תמונת המודעה עוברת מהשורה לדף המודעה
דרך View Transitions API (`src/components/view-transition-link.tsx`).
אותו תקציב זמן, ואותו easing.

`prefers-reduced-motion` מבטל הכול חוץ מ-opacity.

---

## כתיבה בממשק

- **כפתור אומר מה יקרה:** `פרסם מודעה`, לא `המשך`. הטוסט אחריו:
  `המודעה פורסמה`.
- **שגיאה מסבירה מה לעשות:** `המחיר גבוה מהמותר בקטגוריה. הזן עד
  ₪5,000,000.` לא `שגיאה בשדה`.
- **מצב ריק אומר מה להסיר:** `אין תוצאות. נסה בלי "מעלית" — 31 מודעות
  מתאימות.` עם כפתור שמסיר את הפילטר.
- אפס סימני קריאה. אפס אמוג'ים בממשק. אפס "אופס!".

---

## צבעים מחוץ ל-CSS

ארבעה הקשרים לא מריצים CSS ולכן לא יכולים לקרוא טוקן: `next/og` (Satori),
מניפסט ה-PWA, תבניות הדוא"ל, ומאפייני `paint` של MapLibre.

כולם קוראים מ-`src/lib/palette.ts` (`PALETTE` לפנים המכשיר,
`PALETTE_DAY` לפנים היום). **הערכים שם חייבים להישאר תואמים
ל-`globals.css`** — שינוי בצד אחד בלי הצד השני הוא באג, ו-`npm run
check:design` נופל עליו.

---

## RTL

- מאפיינים לוגיים בלבד: `ms-`/`me-`/`start`/`end`. לא `ml-`/`mr-`.
- חצי ניווט הפוכים (חץ "הבא" מצביע שמאלה).
- `<Slider dir="rtl">` — Radix לא יורש כיוון מהמסמך.
- גרפי Recharts עטופים ב-`dir="ltr"` — הספרייה מניחה צירים משמאל לימין.
- מספרים ב-`.num`, ראה למעלה.
