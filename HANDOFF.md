# מסירה — המשך פיתוח לוח

מסמך זה נועד לסשן פיתוח חדש שממשיך את הפרויקט מאפס הקשר.
הוא מתאר **את המצב בפועל**, לא את הכוונות. כל מספר כאן אומת מול הקוד
או מול ריצת CI, ולא הועתק מזיכרון.

---

## 1. מה זה

**לוח** — לוח מודעות ישראלי מלא בעברית ו-RTL. מותג עצמאי; אין בו שימוש
בשם, בלוגו, בצבעים או בטקסטים של אף לוח מודעות קיים, וזו דרישה מחייבת
ולא הערת שוליים.

- ריפו: `aliexpressgood585/Yad-2-`
- ענף פיתוח: `claude/israeli-classifieds-platform-iyb4jf-11kees`
- באוויר: <https://luach-rose.vercel.app>
- מסד: Neon Postgres · פריסה: Vercel · CI: GitHub Actions

---

## 2. כללי עבודה שנקבעו על ידי בעל הפרויקט

הכללים האלה בתוקף לאורך כל הפרויקט ולא פגו:

1. **לא לשאול שאלות.** להכריע לפי best practice, ולתעד את ההכרעה
   ב-`DECISIONS.md`.
2. **לא לתאר תוכניות באמצע — לבצע.**
3. **אסור להשאיר קוד מדומה.** אין `TODO`, אין placeholder, אין פונקציה
   שמחזירה ערך קבוע "לבינתיים". פיצ'ר או שנבנה במלואו ועובד, או שלא
   נבנה ומתועד ככזה בטבלת "מה לא נבנה" ב-`GROWTH.md`. **זה הכלל
   שהפרו אותו הכי בקלות — אל תפר אותו.**
4. בסוף כל שלב: `npm run build`, לתקן שגיאות, `git add -A && git commit`.
5. נתקעת מעל 3 ניסיונות → לכתוב ל-`BLOCKED.md`, לדלג, להמשיך.
6. כל טקסט בממשק בעברית תקנית ו-RTL מלא.
7. `npm run check:all` חייב לעבור לפני כל קומיט. **בדיקה נופלת — תקן
   את הקוד, לא את הבדיקה.**
8. **אין להריץ את workflow הזריעה בלי אישור מפורש לאותה הרצה.** הוא
   מוחק את כל נתוני הפרודקשן.
9. **אין לפתוח Pull Request** אלא אם התבקש במפורש.

---

## 3. סטאק

Next.js 15 App Router · React Server Components · Prisma + PostgreSQL ·
NextAuth · Tailwind · Zustand · TypeScript strict.

חשוב להבין לפני שנוגעים: ISR עם `revalidate` ו-`unstable_cache` בכל
דפי העיון. **אחרי כל זריעה מחדש חייבים לבנות מחדש** — הזריעה מחליפה
כל שורת קטגוריה, והמטמון מצביע על מזהים שכבר לא קיימים. מקומית:
`rm -rf .next` ובנייה. בפרודקשן: פריסה חדשה.

---

## 4. מה בנוי ועובד

| תחום | מצב |
| --- | --- |
| חיפוש עברי | FTS + `pg_trgm` לשגיאות כתיב, 398 מילים נרדפות, מנתח שאילתות שמחלץ מחיר/שנה/יד/עיר מטקסט חופשי |
| פילטרים | שדות דינמיים לכל קטגוריה (EAV) עם ירושה מאב לבן ושדות תלויים (יצרן ← דגם) |
| דף מודעה | גלריה עם zoom, חשיפת טלפון מוגבלת בקצב, מפה מקורבת, היסטוריית מחירים, התפלגות, מודעות דומות |
| מדד מחירים | `/שווי`, `/מחירון/[יצרן]/[דגם]`, `/מחירים/[עיר]` — ISR יומי, מגמת 12 חודשים, JSON-LD |
| פרסום | אשף 4 שלבים, דחיסה בלקוח, הסרת EXIF בשרת, טיוטות, זיהוי כפילויות |
| אמון ובטיחות | ציון סיכון משוקלל, תור מודרציה, דיווחים, `AuditLog` |
| אזור אישי | מודעות, סטטיסטיקות, מועדפים, חיפושים שמורים, צ'אט (פולינג 8 שניות), התראות |
| ניהול | לוח בקרה, מודרציה, ניהול משתמשים |
| התנהגות ממשק | View Transitions בין כרטיס לדף, חיפוש חי, צפיפות תצוגה שנלמדת לכל קטגוריה, כרטיס שמגיב למכל שלו (container queries) |
| התראות | ארבעה טריגרים, קיבוץ, שעות שקט (לילה ושבת לפי שקיעה מחושבת), תור עבודות עמיד עם dedupe ו-backoff, דוח מחירים חודשי |
| מדידה | טבלת אירועים עצמית בלי SDK חיצוני, מדד צפון (מודעות מחוברות בשבוע), משפך חיפוש→צפייה→טלפון→פנייה, `/admin/analytics` |
| סוחרים | תפקידים והרשאות, העלאה מרוכזת מ-CSV, ייבוא פיד (CSV/XML) עם מיפוי אוטומטי, דשבורד ביצועי מלאי |
| הכנסות | הזמנות, מע"מ באגורות, חשבוניות, מנויים, מכסות, זיכוי, והעברה בנקאית כמסלול תשלום אמיתי |
| בדיקות | 18 בדיקות Playwright (דסקטופ ונייד) על חיפוש, פרסום ויצירת קשר; מדידת Lighthouse עם ספים |

---

## 5. מה **לא** בנוי

מפורט בטבלה בסוף `GROWTH.md`. בקצרה:

| סעיף | מצב | החסם האמיתי |
| --- | --- | --- |
| A · פרסום בלחיצה אחת (AI) | **לא נבנה כלל** | אין `ANTHROPIC_API_KEY` בסביבה. לא נכתבה שורה — לא צנרת ולא דגל. ראה `GROWTH.md` סעיף A |
| F · סליקת כרטיסי אשראי | לא נבנה | אין מפתחות ספק. כל מה שסביב בנוי ואומת; החסר הוא קובץ אחד תחת `src/lib/payments/providers` |
| H · i18n | לא נבנה | היקף: ~1,150 מחרוזות ב-154 קבצים. אין חסם חיצוני — נדחה מפני שאר הסעיפים |

סעיפים B, C, D, E, G, ורוב H — הושלמו ואומתו בהרצה מקומית מלאה.

**אל תסמן אף אחד מאלה כ"בוצע" בלי שהוא באמת רץ מקצה לקצה.**

---

## 6. מסמכים — לקרוא לפני נגיעה בקוד

| קובץ | מה יש בו |
| --- | --- |
| `DECISIONS.md` | 56 הכרעות עיצוב וארכיטקטורה עם הנימוק לכל אחת. **קרא לפני שאתה משנה משהו שנראה שרירותי — כנראה הוא לא.** |
| `DESIGN.md` | מערכת העיצוב. כל טוקן, וחוק ברזל: צבע פעולה אחד בלבד למסך |
| `SEARCH.md` | מנוע החיפוש העברי, הנרמול, המילים הנרדפות ומנתח השאילתות |
| `GROWTH.md` | סעיפי הצמיחה, מה בוצע ומה לא ולמה |
| `BLOCKED.md` | מגבלות מודעות שנבחרו במכוון (אחסון, rate limit, סליקה, פולינג) |

---

## 7. פקודות

```bash
npm run dev            # פיתוח
npm run build          # prisma generate && next build
npm run typecheck      # tsc --noEmit
npm run lint
npm run check:all      # תשע בדיקות טהורות, בלי בסיס נתונים
npm run check:seed     # תקינות הנתונים שנזרעו — דורש בסיס נתונים זרוע
npm run e2e            # בדיקת קבלה מקצה לקצה (דורש E2E_CATEGORY_ID ושרת dev)
npm run e2e:ui         # Playwright — 18 בדיקות, דסקטופ ונייד
npm run lighthouse     # מדידה מול בנייה של פרודקשן בלבד
npm run db:migrate     # מיגרציה בפיתוח
npm run db:seed        # זריעה
```

`npm run check:all` הוא הרשת הביטחונית האמיתית של הפרויקט. הבדיקות
האלה כבר תפסו באגים אמיתיים — למשל שמשקל `PRIOR_REPORTS` היה מתחת לסף
האזהרה. **אם בדיקה נופלת, תקן את הקוד ולא את הבדיקה.**

---

## 8. פריסה וזריעה

הסביבה שבה רץ הסוכן חוסמת גישה יוצאת לכל דומיין חוץ מ-`api.github.com`.
**לא ניתן לגשת ל-Vercel, ל-Neon או לאתר החי מתוך הסשן.** לכן כל פעולה
בפרודקשן עוברת דרך GitHub Actions:

- `.github/workflows/deploy.yml` — פריסה
- `.github/workflows/seed.yml` — זריעה ידנית בלבד. **הרסנית**: מוחקת
  הכל וזורעת מחדש. חסומה מאחורי הקלדת `WIPE`, דורשת את הסוד
  `NEON_DIRECT_URL`, ודוחה מחרוזת חיבור מסוג pooler.

אחרי זריעה — לדחוף קומיט כדי לאלץ בנייה מחדש, אחרת המטמון מציג 0 מודעות.

**אל תריץ את הזריעה בלי אישור מפורש של בעל הפרויקט לאותה הרצה.**

---

## 9. מלכודות אמיתיות שכבר עלו בדם

- **`\b` ברג'קס לא עובד על עברית.** הוא מוגדר מול `\w` שלא כולל אותיות
  עבריות, ולכן `/\bמיליון\b/` לעולם לא מתאים. יש להשתמש בגבולות
  מפורשים — ראה `src/lib/search/parse-query.ts`.
- **Prisma שולח מספרי JS כ-bigint.** פונקציות Postgres כמו `width_bucket`
  נופלות בלי `::float` / `::int` מפורש.
- **Satori (`next/og`) לא פותר נתיבי תמונה יחסיים ולא משתני CSS.** יש
  להשתמש ב-`absoluteUrl()` מ-`src/lib/site.ts`.
- **מפות נרמול חייבות להיות מנורמלות בעצמן.** מפתח לא מנורמל במפה
  נכשל בשקט מול קלט מנורמל.
- **Neon בתוכנית החינם משעה את המחשוב.** `P1001` חולף — פשוט להריץ שוב.
- **Vercel Hobby מרשה cron יומי בלבד.** `vercel.json` מאחד הכל
  ל-job יומי אחד.
- **בדיקה מול שרת ישן.** `EADDRINUSE` פירושו שהתהליך הקודם שרד ואתה
  בודק בנייה ישנה. לוודא לפני שמאשימים את הקוד. `pkill` בתוך פקודה
  מורכבת הורג גם את השרת החדש שהרגע עלה — להשתמש ב-`fuser -k 3000/tcp`.
- **`cookies()` ו-`headers()` אסורים בתוך `after()`.** Next בולע את
  השגיאה ל-stderr, והאירוע פשוט לא נכתב. לקרוא את הערך **לפני**
  ה-callback ולהעביר אותו פנימה — ראה `browse-view.tsx`.
- **עוגייה שנכתבת רק על התגובה אינה נראית לרכיבי שרת באותה בקשה.**
  ה-middleware מוסיף אותה גם לכותרות הבקשה המועברות, אחרת האירוע הראשון
  של כל מבקר חדש אובד.
- **ענף השקיעה בחישוב הזריחה.** `(360 - acos(cosH))/15` הוא הזריחה;
  השקיעה היא `acos(cosH)/15`. הבלבול נתן "שקיעה" ב-06:40 ושלח את כל
  ההתראות לתוך שעות השקט.
- **`prisma migrate dev` דורש טרמינל אינטראקטיבי** ואינו זמין כאן.
  העוקף: `migrate diff --script` לתיקיית מיגרציה שנוצרת ביד, ואז
  `migrate deploy`.
- **`matchAll` יחיד על `<name>…</name>` בולע פריטים מקוננים** והחזיר
  שורה אחת משני פריטים בפיד. לסרוק כל שם אלמנט בנפרד.
- **הזריעה אינה מאמתת את עצמה** מול הגדרות השדות. `check:seed` כן, והוא
  רץ עכשיו בתוך workflow הזריעה.

---

## 10. עיצוב — ההכרעה התקבלה ומומשה

בעל הפרויקט הכריע: **כיוון א (מכשיר) כבסיס לכל האתר, כיוון ג (קרינה)
כשכבת תמונה בתוך אותו שלדה.** מומש במלואו. `DESIGN.md` הוא המקור.

מה שנובע מזה, ולא לשנות בלי לקרוא את `DECISIONS.md` קודם:

- **אפס פינות מעוגלות ואפס צללים.** מיושם ב-`tailwind.config.ts`: כל
  טוקן `borderRadius` מצביע ל-`var(--radius)` שהוא `0px`, וכל טוקן
  `boxShadow` הוא `none`. לכן ~200 שימושי `rounded-*` קיימים בקוד
  ואינם עושים דבר — אין צורך למחוק אותם, ואין טעם להוסיף.
- **ענבר הוא צבע הקריאה היחיד; ציאן רק להכרעה.** חוק הברזל של צבע
  פעולה אחד למסך נשאר בתוקף.
- **מתחת ל-8 מודעות להשוואה — אין מחוג בכלל.** לא מחוג חלש ולא אפור.
  `PriceGauge` מחזיר `null`. מכשיר שמציג קריאה שאין לו הוא מכשיר שבור.
- **צבע הכרטיס נדגם מהתמונה** דרך `src/lib/listing-accent.ts`, עם רצפת
  רוויה ובדיקת ניגודיות 3:1. הפונקציה מחזירה שני ערכים — אחד למצב
  מכשיר ואחד למצב יום — משום שבהירות אחת אינה יכולה לעבור ניגודיות מול
  שני רקעים.
- `--listing-accent` מוצהר על `.listing-row` ועל `.accent-scope`,
  **לא** על `.dark`. הצהרה על `.dark` גורמת לכל שורה ליפול חזרה
  ל-`--rule` בשקט.
- `npm run check:design` שומר על הפלטה. הוא כבר תפס סטייה אמיתית
  (`--chassis` נכתב `217` במקום `218`).

---

## 11. אי-דיוקים ידועים

אין. המספרים ב-`README.md` תוקנו ונבדקים אוטומטית מול הקוד ב-
`npm run check:docs`, שהוא חלק מ-`check:all`.
