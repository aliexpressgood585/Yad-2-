# SEO — מה להגיש, ובאיזה סדר

מסמך תפעולי, לא מדריך. הסדר כאן הוא סדר ביצוע.

## 0. לפני הכול

```bash
npm run demo:purge            # מודעות דמו לא נכנסות לאינדקס
curl -s https://<דומיין>/api/health   # database ו-storage חייבים להיות ok
```

אתר שנסרק עם 3,000 מודעות דמו ואז מתנקה מקבל 3,000 שגיאות 404
ב-Search Console, ולוקח שבועות עד שהן נעלמות. הניקוי הוא לפני
ההגשה, לא אחריה.

## 1. אימות הדומיין ב-Search Console

הוסיפו נכס מסוג **Domain** (לא URL prefix) ואמתו ברשומת TXT ב-DNS.
נכס Domain מכסה גם `www` וגם `https` וגם תת-דומיינים — עם URL prefix
תגלו בעוד חודש שחצי הנתונים בנכס אחר.

## 2. הגשת מפת האתר

הגישו כתובת אחת:

```
https://<דומיין>/sitemap/0.xml
```

זו המפה הכללית — דפים סטטיים, קטגוריות ודפי מחירון. גוגל מגלה משם
את מפות הקטגוריות (`/sitemap/1.xml` ואילך) דרך `robots.txt`.

הפיצול לפי קטגוריה אינו בגלל מגבלת הגודל אלא בגלל האבחון: מפה אחת
ענקית אומרת "משהו לא נאסף", מפה לקטגוריה אומרת **איזו**.

## 3. בדיקת הנתונים המובנים

`https://search.google.com/test/rich-results` על שלושה סוגי דפים:

| דף | מה חייב להופיע |
| --- | --- |
| מודעה | `Product` + `Offer` + `BreadcrumbList` |
| קטגוריה | `BreadcrumbList` |
| בית | `Organization` + `WebSite` עם `SearchAction` |

`Offer` בלי `price` תקין או בלי `availability` נפסל בשקט — הדף
נסרק, פשוט בלי התוצאה העשירה.

## 4. סדר הדפים שכדאי לדחוף

לא לפי מה שחשוב לנו אלא לפי מה שיש עליו ביקוש ואין עליו תחרות:

1. **`/price-guide/<יצרן>/<דגם>`** — "כמה שווה מאזדה 3 2019" הוא
   חיפוש עם כוונת קנייה גבוהה שהלוחות הגדולים לא עונים עליו טוב.
   זה הנכס היחיד שמצטבר ולא נגמר כשמפסיקים לשלם.
2. **`/city-prices/<עיר>`** — אותו היגיון בנדל"ן.
3. **`/valuation`** — דף הכלי עצמו.
4. **קטגוריות** — תחרות גבוהה, אבל הן הבסיס למבנה האתר.
5. **מודעות** — נסרקות ממילא דרך המפות, ורובן פגות תוך 45 יום.

## 5. מה לבדוק אחרי שבועיים

- **Coverage** — כמה כתובות נאספו מכל מפה. פער גדול בקטגוריה אחת
  מצביע על בעיה באותה קטגוריה, לא באתר.
- **Core Web Vitals** — הנתון האמיתי מגיע משדה ולא מ-Lighthouse.
- **Performance → Queries** — אילו שאילתות מביאות חשיפות. אם רובן
  שם המותג, ה-SEO עדיין לא עובד; מותג חדש אמור להביא מעט מאוד.

## 6. מה לא לעשות

- לא לרכוש קישורים. גוגל מזהה, והעונש על לוח חדש כבד.
- לא ליצור דפי "מודעות ב<עיר> ב<קטגוריה>" לכל צירוף אפשרי. דפים
  דקים בהיקף גדול הם בדיוק מה ש-Helpful Content מדרג למטה.
- לא לחסום `/search` בטעות — הוא כבר חסום ב-`robots.txt` בכוונה, כי
  דפי תוצאות מייצרים תוכן כפול.
